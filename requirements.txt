# 🚀 Основной веб-фреймворк
fastapi==0.115.12              # Современный асинхронный фреймворк для API

# ⚙️ ASGI-сервер для запуска FastAPI
uvicorn==0.34.2                # Лёгкий сервер для запуска FastAPI-приложений

# 💾 Работа с PostgreSQL
psycopg2-binary==2.9.10        # Драйвер для подключения к PostgreSQL (без бинарной версии)

# 🧱 ORM
SQLAlchemy==2.0.41             # ORM для работы с базами данных

# 📦 Валидация и сериализация данных
pydantic==2.11.4               # Библиотека для валидации данных (используется в FastAPI)
pydantic_core==2.33.2          # Ядро pydantic v2 (автоматически ставится вместе с ним)

# ⚡️ Работа с async I/O
anyio==4.9.0                   # Обертка для asyncio/curio/trio
sniffio==1.3.1                 # Для определения текущего async-бэкэнда

# 🔧 Веб-протоколы и утилиты
h11==0.16.0                    # HTTP/1.1 протокол (используется в Uvicorn)
httptools==0.6.4               # Ускоренный HTTP парсер (опционально для Uvicorn)
uvloop==0.21.0                 # Быстрый event loop (опционально для Uvicorn)
watchfiles==1.0.5              # Автоматическая перезагрузка при изменениях
websockets==15.0.1             # Поддержка WebSocket (используется в Uvicorn)
starlette==0.46.2              # ASGI-надстройка (FastAPI использует под капотом)

# 🧪 Аннотированные типы
annotated-types==0.7.0         # Поддержка Annotated из typing

# ⚙️ CLI и расширения
click==8.2.1                   # Удобное создание CLI-команд

# ⚙️ Переменные окружения
python-dotenv==1.1.0           # Загрузка .env-файлов

# 📄 Поддержка YAML
PyYAML==6.0.2                  # Чтение и запись YAML-файлов

# 🧠 Поддержка типизации
typing-inspection==0.4.1       # Для работы с типами в runtime
typing_extensions==4.13.2      # Поддержка новых фич typin_
